import { FormatTypeUserPipe } from './format-type-user.pipe';

describe('FormatTypeUserPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatTypeUserPipe();
    expect(pipe).toBeTruthy();
  });
});
